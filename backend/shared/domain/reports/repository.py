from typing import Protocol
from .contracts import ReportPage, ReportSnapshot

class ReportRepository(Protocol):
    def get(self, identifier: str, actor_id: str) -> ReportSnapshot | None: ...
    def list_for_owner(self, actor_id: str, cursor: str | None = None, limit: int = 50) -> ReportPage: ...
