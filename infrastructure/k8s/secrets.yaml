apiVersion: v1
kind: Secret
metadata:
  name: allaeeb-secrets
  namespace: allaeeb
type: Opaque
# Values are injected at deployment by an approved secret manager. Never commit
# credentials, sample passwords, API keys, or base64-encoded values in this file.
data: {}
